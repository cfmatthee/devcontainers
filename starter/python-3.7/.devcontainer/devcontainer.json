{
  "name": "<app name>",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/python",
  "extensions": ["ms-python.python"],
  "postCreateCommand": "pip install -r dev-requirements.txt && pip install -r requirements.txt"
}
